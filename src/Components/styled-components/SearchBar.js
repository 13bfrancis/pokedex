import styled from 'styled-components';

const SearchBar = styled('input').attrs({})`
  margin: 10px;
  font-size: 25px;
  text-align: center;
  flex-basis: 75%;
  height: 50px;
`;

export default SearchBar;
